#include "holberton.h"
#include <stdio.h>

/**
 * main - program entry
 * @argc: number of arguments
 * @argv: argument text
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);
	return (0);
}
