#include "holberton.h"
#include <stdio.h>

/**
 * main - program entry
 * @argc - argument count
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if(argc && argv)
		printf("%d\n", argc);

	return (0);
}
